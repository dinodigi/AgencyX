# Desktop dev env (loaded by `pnpm dev`). Copy to apps/desktop/.env.
# Delivery-scoped AgentX project token — lets sign-in + sync reach AgentX.
# Mock scrape runs work WITHOUT this (leads just won't sync).
AGENTX_DELIVERY_TOKEN=agx_...
