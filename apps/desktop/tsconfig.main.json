{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    // DOM lib for the page.evaluate() bodies in the scraper (they run in-browser).
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src/main", "src/preload", "src/shared"]
}
